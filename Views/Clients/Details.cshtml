@model HairSalon.Models.Client

<h2>Client Details</h2>

<div>
    <h3>@Model.Name</h3>
    <p>Stylist: @Model.Stylist.Name</p>
    <!-- Add other client details here as needed -->
</div>

<p>
    <a href="@Url.Action("Edit", new { id = Model.Id })" class="btn btn-primary">Edit</a>
    <a href="@Url.Action("Delete", new { id = Model.Id })" class="btn btn-danger">Delete</a>
    <a href="@Url.Action("Index", "Stylists")" class="btn btn-secondary">Back to Stylist List</a>
</p>
